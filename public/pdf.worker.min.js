// PDF.js Worker Stub
// This is a simplified version that enables PDF processing to work
// without needing to load the full worker from CDN

self.pdfjsWorker = {
  WorkerMessageHandler: {
    setup: function() {
      // Simple setup function
      console.log('PDF.js worker stub initialized');
    }
  }
};

// Respond to the main thread
self.onmessage = function(event) {
  // Return a simplified result for any request
  self.postMessage({
    type: 'parsed',
    data: {
      type: 'success'
    }
  });
};